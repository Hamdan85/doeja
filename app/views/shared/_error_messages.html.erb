<% if @Receiver.errors.any? %>
    <div class="errorwindow" >
      <h1>Encontrados Erros:</h1>
      </br>
      <ul>
        <% @Receiver.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>